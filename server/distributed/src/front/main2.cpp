#include "BWServer.h"
//#include <QDebug>
#include <iostream>
int main(int argc, char *argv[]) {
  std::cout <<"Initializing BW server" << std::endl;
/*  BWServer bwServer;
  std::string featureServerAddr(argv[2]); // = "0.0.0.0:50051";
  if (!bwServer.init(featureServerAddr)) {
    qCritical() << "Starting HTTP Server failed";
    return 1;
  }

  std::string bwServerAddr(argv[1]); // = "0.0.0.0:50056";
  bwServer.run(bwServerAddr);
*/
  return 0;
}

